# 待办事项与配置说明 (TODO)

## 遗留项 (Pending Next Steps)

- [ ] **多日维度的前端图表**: 现阶段已为后端埋好了 `daily_statistics` （农场增量表），可供后续在 Analytics.vue 内提供周/月维度的折线图绘制。目前未包含于本次需求内。
- [ ] **异常重试策略接入 API**: `accounts` 表现已拥有 `api_error_count` 字段，需在未来 API 层或 Runtime Engine 中将网络不可达 / Token 失效产生的 Error 按频率回写该字段，以辅助自动休眠等封禁保护逻辑。
- [ ] **高级配置向前端暴露**: 后端已完成 `advanced_settings` 的存取与序列化逻辑，将来如果产生边缘功能的拓展，可以利用它在修改 Settings.vue 存入嵌套 JSON 而非再次进行繁琐的数据表迁移。

## 操作向导与环境说明

1. **环境检查**：本次引入的迁移能力将会在应用首次启动时接管数据库建表，你只需如同往常一样**重启后端应用**即可：`npm run start` 等或者在 PM2 面板中重启服务。
2. **清理观测**：可等待 24 小时验证自动化数据切割以及 7 天后数据库文件的稳定情况，理论上将会从不断攀升变成峰值震荡（安全范围）。
3. **前端重构**：若你在其他视图还需要记忆什么参数，可以使用直接在相应的 `.ts/.vue` 下调用 `useStorage('key-name', default_value)`，无需重复造轮子。
