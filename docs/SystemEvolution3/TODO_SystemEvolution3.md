# 后续计划与遗留事项 (TODO) - 系统演进 Phase 3

## 下阶段建议 (Phase 4 展望)

随着前面三个 Phase 的依次落定（任务队列改造、平台隔离工厂重构、大型上帝类降解拆分、数据库锁防冲突、全局安全日志监控），当前 QQ 农场协议的底盘已经相当牢固了。

如果有下一迭代，建议进军：
1. **自动风控熔断机制 (Automated Circuit Breaker)**
   - 根据 `system_logs` 表的统计频率，做一个本地守护进程，当判断到由于外部协议变动触发大规模 `banned` 或 `error` 时，触发应用全局熔断并暂停所有队列。

2. **跨库数据可视化 (Vue Echarts 接入)**
   - 我们现在有 `stats` 日态追踪，可以把各账号每天偷偷摸摸赚的经验、金币以大屏（折线图）的形式直观展示。

3. **Farm Tools 生态完善 (API 转写)**
   - `admin.js` 内现存有很多遗留的 `/api/calculator` 等基于本地算力的逻辑，可以抽出到独立的 `farm-routing.js` 下面并优化内存调度。

## 开发者配置留意
- `web/src/views/SystemLogs.vue` 为新界面入口，如果页面刷新显示“路由丢失”，属于正常缓存现象，`npm run dev` 即可重载。 
- 若出现任何不明异常变量，直接检查 `tmp/fix_imports.js` 的引用链路即可。
